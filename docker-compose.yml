version: "4.28"

services:
  backend:
    build:
      context: .
      dockerfile: FastAPI_Backend/Dockerfile
    image: backend:latest
    ports:
      - 8081:8081
    networks:
      - project_network

  frontend:
    build:
      context: .
      dockerfile: React_Frontend/Dockerfile
    image: frontend:latest
    ports:
      - 5173:5173
    networks:
      - project_network
    depends_on:
      - backend
    volumes:
      - ./React_Frontend:/app/backend

networks:
  project_network: